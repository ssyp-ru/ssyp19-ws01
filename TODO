Миша. Мерджи checkout в мастер. После этого сделай git add, git commit (это вроде легко) и сделай, чтобы checkout работал на объектах коммита (и только на них). Далее нужно делать git branch. Подумай как это нужно делать. Для этого тебе нужны операции типа update-ref, уточни у Димы.
ps. Ты же помнишь как делать коммит (я знаю, что ты умный, но на всякий случай подсказка: в HEAD и в refs/heads/master есть sha от которого нужно идти, посмотри что происходит при коммите в реальном гите).

Данил. Я там вроде поправил немного, мерджи в мастер. Но оно не до конца работает. Доработай. Обработай случаи, когда пользователь вводит фигню! Далее нужно уметь читать объект коммита из файла (нужно делать git log). Ещё сильно надо, чтобы выводились help по командам, если передан аргумент --help, но это менее приоритетно, чем коммиты.

Вообще, коллеги, у нас если что-то не то происходит, пользователю никак не сообщается. Ошибки обрабатываем, но один хрен игнорируем. Нужно выводить на экран!

Дима. Я там порефакторит твой код (не знаю как ты раньше с ним работал) + поправил кой чего. Но чтобы тебе не стало скучно, некоторые ошибки я оставил. Таблица вроде делается праивльно, но в way_back ошибки. Развлекайся.

Если кто заскучает, может попробовать найти ошибку в string_t (valgrind вам в помощь). Приду часов в 11-12.
